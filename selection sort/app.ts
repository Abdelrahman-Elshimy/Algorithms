// Selection Sort

// ! Slow

let arr: number[] = [4, 3, 42, 54, 11, 76];
var last = 0;
for (let index = 0; index < arr.length; index++) {
  let min = 0;
  if (min > arr[index]) {
    min = arr[index];
  }
  console.log(min);
}
